Fixed Oracle CI by addressing three residual issues from CI run 31337577220:

1. Added the missing ``pytest-benchmark`` dependency to ``requirements-dev.txt``. The ``tests/benchmark/relation/test_cache_comparison.py`` module references the ``benchmark`` fixture from ``pytest-benchmark``, which was not declared and made every test in that file error out at setup with ``fixture 'benchmark' not found``. Mirrors the dependency list of the MySQL backend.

2. Routed the ``order_item_class`` / ``async_order_item_class`` fixtures (used by every composite-PK query/cte/set-operation test) to load the dedicated ``feature/composite_pk/schema/order_items.sql`` schema. The composite-PK ``OrderItem`` model from ``basic.fixtures.models`` declares a composite primary key on ``(order_id, product_id)`` with no surrogate ``id``; the previous code path loaded ``feature/query/schema/order_items.sql`` which uses an ``id`` surrogate and extra ``product_name``/``subtotal`` columns, so DDL succeeded but the model could not insert a row.

3. Restored ``feature/query/schema/order_items.sql`` to the auto-increment-id schema that matches the regular ``OrderItem`` model used by ``setup_order_fixtures`` (query/feature/basic/cte tests). The previous composite-PK rewrite broke those ``OrderItem`` INSERTs with ``ORA-00904: \"ID\": invalid identifier`` because the column was no longer present.
