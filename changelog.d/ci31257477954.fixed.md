Fixed Oracle backend raising ORA-00923 ("FROM keyword not found where expected") when `find_all(derived=True)` or any query mixing `SELECT *` with additional expression columns was issued. The dialect now qualifies the bare `*` with the FROM source's table alias when the SELECT list contains both a wildcard and other expressions.
