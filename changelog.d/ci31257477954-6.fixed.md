Fixed `OracleBackend.connect()` / `AsyncOracleBackend.connect()` silently orphaning the previous server-side session when invoked twice on the same backend instance. The methods are now idempotent: any pre-existing connection is closed before opening a new one, eliminating one source of dispatcher/session exhaustion (ORA-12516 / DPY-6005) during long-running pytest runs.
