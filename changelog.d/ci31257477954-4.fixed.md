Fixed Oracle `IntegrityError` for NOT NULL constraint violations (`ORA-01400`) lacking the "cannot be null" / "violates not-null constraint" phrase that testsuite pattern matches expect. The `_handle_error` paths and raw `execute`/`executescript`/bulk INSERT/async execute paths now prepend `cannot be null:` to the ORA-01400 message so cross-backend message probes match uniformly.
