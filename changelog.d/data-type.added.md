Adapted Oracle backend to core DataType refactoring (#108): 13 `Oracle*Type(backend="oracle")` DataType subclasses with `synonyms()`, `DDLColumnMixin` using `data_type.to_sql()`, `OracleTypeSupportMixin`, and introspector `ColumnInfo.parsed_data_type` population. Replaced pure-string SchemaDiff comparison with core `SchemaDiffer._columns_equivalent` using `parsed_data_type.is_equivalent()`. Added `examples/schema_diff/`.
