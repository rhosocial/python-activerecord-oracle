# docker-compose.yml
# Oracle Database containers for development and testing
services:
  oracle_18c:
    image: registry.cn-shanghai.aliyuncs.com/vistart_public/oracle-xe:18-slim
    container_name: rhosocial_oracle_18c
    ports:
      - "11521:1521"
    environment:
      - ORACLE_PASSWORD=Password1!
      - ORACLE_CHARACTERSET=AL32UTF8
      - APP_USER=testuser
      - APP_USER_PASSWORD=Password1!
    volumes:
      - oracle_18c_data:/opt/oracle/oradata
    networks:
      - rhosocial_net
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s

  oracle_21c:
    image: registry.cn-shanghai.aliyuncs.com/vistart_public/oracle-xe:21-slim
    container_name: rhosocial_oracle_21c
    ports:
      - "11522:1521"
    environment:
      - ORACLE_PASSWORD=Password1!
      - ORACLE_CHARACTERSET=AL32UTF8
      - APP_USER=testuser
      - APP_USER_PASSWORD=Password1!
    volumes:
      - oracle_21c_data:/opt/oracle/oradata
    networks:
      - rhosocial_net
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s

  oracle_23c:
    image: registry.cn-shanghai.aliyuncs.com/vistart_public/oracle-free:23-slim
    container_name: rhosocial_oracle_23c
    ports:
      - "11523:1521"
    environment:
      - ORACLE_PASSWORD=Password1!
      - ORACLE_CHARACTERSET=AL32UTF8
      - APP_USER=testuser
      - APP_USER_PASSWORD=Password1!
    volumes:
      - oracle_23c_data:/opt/oracle/oradata
    networks:
      - rhosocial_net
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 120s

volumes:
  oracle_18c_data:
  oracle_21c_data:
  oracle_23c_data:

networks:
  rhosocial_net:
    driver: bridge