# schema tests

SchemaSupport capability for the Oracle dialect: Oracle namespaces objects per user schema, so schema-qualified names work while CREATE SCHEMA-style DDL does not apply.

## Key files

- `test_schema_support.py` — SchemaSupport capability semantics
