# dialect tests

Currently empty. All former Oracle-specific dialect tests were re-homed to `oracle/` (10 vendor expression suites, dialect security, PGQ formatting) and the generic dialect regressions to `ddl/` / `dml/` per the cross-backend test taxonomy plan §5.5. Generic dialect subjects for Oracle remain a Tier-2 fill gap.
