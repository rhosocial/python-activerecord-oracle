CREATE TABLE profiles (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    bio VARCHAR2(1024) NOT NULL,
    author_id NUMBER(10) NOT NULL,
    CONSTRAINT fk_profiles_author FOREIGN KEY (author_id) REFERENCES authors(id) ON DELETE CASCADE
)
